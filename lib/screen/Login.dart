import 'package:flutter/material.dart';
import 'package:flutterloginapp/components/loginbody.dart';

class Login extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: LoginBody(),
    );
  }
}
